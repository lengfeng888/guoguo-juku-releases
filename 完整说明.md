# 果果剧库完整说明

本文档面向部署、日常使用、二次开发和运维人员，说明果果剧库的功能范围、运行方式、数据目录、TVBox 接入、播放画质、图片代理、Docker、Emby 同步、安全注意事项和常见故障处理。

项目仅用于管理你有权访问和播放的内容。请遵守内容来源的服务条款、版权要求和所在地区法律。

## 1. 项目概览

果果剧库是一个 Go 编写的短剧管理与在线播放服务，包含：

- 剧库、搜索、榜单、推荐、追剧和历史记录
- 在线播放、续播、画质选择、倍速、选集和移动端手势
- TVBox 接口、TVBox 配置和最高画质播放
- 下载任务、并发控制、断点重试和视频合并
- Emby STRM / NFO 导出、自动同步和海报同步
- 多账号、权限、站源限制和仅在线观看模式
- 内置 Web UI，不需要单独运行 Node.js 或 Python
- FFmpeg 自动发现或便携安装
- Windows、macOS、Linux 多平台构建

编译后的程序已经嵌入网页、JavaScript、CSS 和图标资源，部署时只需要一个可执行文件；下载、解密、封装或转码时再由 FFmpeg 处理媒体。

## 2. 运行要求

### 2.1 使用预编译程序

- Windows x64 / ARM64
- macOS Intel / Apple Silicon
- Linux x86-64 / ARM64
- 程序所在目录必须可写
- 需要保留和备份同目录下的 `data/`
- 视频下载需要足够的磁盘空间
- 局域网或公网访问需要放行 TCP 端口，默认是 `8999`

### 2.2 从源码编译

- Go 1.21 或更高版本
- 支持的目标平台由 `GOOS` 和 `GOARCH` 决定
- Docker 构建使用仓库内的 `Dockerfile`
- 前端资源已经随 Go 程序嵌入，不需要 npm 构建

检查 Go：

```bash
go version
```

## 3. 目录结构

```text
.
├── main.go
├── go.mod
├── internal/
│   ├── app/              Go 服务、播放、下载、TVBox、Emby、账号逻辑
│   └── webui/            内置网页、样式和 JavaScript
├── scripts/
│   ├── build-all.sh      六平台构建脚本
│   ├── ____.command      macOS 启动脚本
│   ├── ____.sh           Linux 启动脚本
│   └── ____.bat          Windows 启动脚本
├── Dockerfile
├── compose.yaml
├── docs/
└── README.md
```

运行后还会生成：

```text
data/
├── juku.json             基础配置
├── playback-settings.json 播放和资源限制
├── accounts.json         账号、权限和访问策略
├── viewer-key            身份签名密钥
├── viewers/              用户或匿名身份的观看记录
├── library.json          剧库缓存和分页位置
├── ui-state.json         UI、下载和画质状态
├── emby-key              Emby 签名密钥
└── logs/app.log          自动轮转的诊断日志
短剧下载/                 默认视频输出目录
bin/                      FFmpeg 等便携运行组件
```

`data/`、下载目录、日志、便携二进制和构建产物不应上传到 Git 仓库。

## 4. 快速启动

### 4.1 macOS

```bash
chmod +x ./juku_darwin_arm64
./juku_darwin_arm64 -open=false
```

Apple Silicon 使用 `juku_darwin_arm64`，Intel 使用 `juku_darwin_amd64`。

### 4.2 Linux

```bash
chmod +x ./juku_linux_amd64
./juku_linux_amd64 -open=false
```

ARM64 使用 `juku_linux_arm64`。

### 4.3 Windows

双击：

```text
juku_windows_amd64.exe
```

或在 PowerShell 中运行：

```powershell
.\juku_windows_amd64.exe -open=false
```

### 4.4 源码运行

```bash
go env -w GOPROXY=https://goproxy.cn,direct
go env -w GOSUMDB=off
go run .
```

默认访问地址：

```text
http://127.0.0.1:8999
```

同一局域网的其他设备使用：

```text
http://运行程序电脑的局域网IP:8999
```

服务器部署建议使用：

```bash
./juku_linux_amd64 -listen 0.0.0.0:8999 -open=false
```

只允许本机访问：

```bash
./juku_darwin_arm64 -listen 127.0.0.1:8999 -open=false
```

## 5. 管理员和账号

第一次启动会创建管理员 `admin`。如果没有显式指定密码，程序会生成随机初始密码并输出到启动终端。首次登录必须修改密码。

创建或更新管理员：

```bash
./juku -admin-user owner -admin-password '替换为强密码'
```

也可以使用环境变量：

```bash
export JUKU_ADMIN_USER=owner
export JUKU_ADMIN_PASSWORD='替换为强密码'
./juku -open=false
```

管理员可以：

- 设置是否必须登录
- 设置是否允许自行注册
- 创建普通账号
- 指定账号可访问的站源
- 启用仅在线观看权限
- 修改代理、下载目录、并发和 FFmpeg
- 配置 Emby 同步
- 查看和管理任务

密码使用加盐派生方式保存，不保存明文。修改密码会撤销该账号其他设备的登录状态。

## 6. Web UI 使用

主要页面：

- 剧库：浏览、搜索、排序、详情、收藏、下载和播放
- 追剧：想看、在看、已看和新集提示
- 历史记录：续播位置、观看进度和清除记录
- 下载：任务进度、暂停、恢复、重试、取消和合并
- 更多 → 站源状态：逐站源更新目录和资料
- 更多 → 代理 / 设置：网络、下载、播放和资源限制
- 更多 → 用户管理：账号、权限和访问策略
- 更多 → Emby 同步：STRM、NFO 和海报同步

点击剧名打开详情，点击海报直接播放。播放进度按剧集 ID 和位置保存，重新打开后可以从上次位置继续。

## 7. 剧库和资料更新

“更新剧库”同时处理：

- 查找新条目
- 从已保存分页位置继续加载历史目录
- 查询缺失的剧名、简介、集数、分类和封面
- 保留已有条目，不因为单页失败清空剧库

当前缓存数量不等于源站总量。首次加载或失败重试时，需要重复点击更新，程序会从检查点继续。

站源可以单独更新。更新过程不会改变当前筛选条件，已有内容在更新期间仍然可以使用。

## 8. 在线播放

播放器支持：

- 播放、暂停、快进、后退、音量
- 倍速和临时 3 倍速
- 自动下一集
- 选集、跳集和每页 30 集
- 全屏、横屏和方向感应
- 移动端上下滑动切集
- 播放进度自动保存和断线恢复
- 画质列表和播放中切换画质

默认播放链路会解析源站实际可用画质，输出保留源分辨率，不会把低清片源强行放大。可选“优先原始 MP4 / HLS 播放”，兼容片源可以跳过 FFmpeg；只有需要解密、换封装或转码时才启动媒体处理。

播放会话会定期保活。浏览器短时间断线后可以自动恢复到同一剧、同一集和原位置。登录失效、账号权限变化或持续网络失败时，需要重新登录或手动重试。

## 9. TVBox 接入

TVBox 不同版本的设置项名称不完全一样，先区分三个地址用途：

| TVBox 设置项 | 使用地址 | 用途 |
| --- | --- | --- |
| 配置地址 / 订阅地址 | `/tvbox.json` | 推荐使用，返回完整站点配置 |
| 点播接口 / 接口 | `/api/tvbox` | 直接添加果果剧库站点 |
| 兼容接口 | `/api.php/provide/vod` | 给需要苹果 CMS 风格地址的客户端使用 |

### 9.1 三种地址格式

当前电脑本机访问：

```text
配置地址：http://127.0.0.1:8999/tvbox.json
点播接口：http://127.0.0.1:8999/api/tvbox
兼容接口：http://127.0.0.1:8999/api.php/provide/vod
```

同一局域网内的电视、手机或 TVBox：

```text
配置地址：http://运行程序电脑的局域网IP:8999/tvbox.json
点播接口：http://运行程序电脑的局域网IP:8999/api/tvbox
兼容接口：http://运行程序电脑的局域网IP:8999/api.php/provide/vod
```

当前 Mac 实测局域网地址为 `192.168.10.7`，所以现在可以填写：

```text
http://192.168.10.7:8999/tvbox.json
```

局域网 IP 可能因路由器 DHCP 或切换网络而变化。可以在 Mac 终端查：

```bash
ipconfig getifaddr en0
```

如果 Wi-Fi 不是 `en0`，再试 `en1` 或 `en2`。

公网域名或 HTTPS 反向代理：

```text
配置地址：https://tv.example.com/tvbox.json
点播接口：https://tv.example.com/api/tvbox
兼容接口：https://tv.example.com/api.php/provide/vod
```

反向代理到子路径时，前缀必须保持一致：

```text
配置地址：https://example.com/juku/tvbox.json
点播接口：https://example.com/juku/api/tvbox
兼容接口：https://example.com/juku/api.php/provide/vod
```

### 9.2 TVBox 填写步骤

1. 确保程序正在运行，并且监听 `0.0.0.0:8999`。只想本机使用时才是 `127.0.0.1:8999`。
2. 确保 TVBox 和运行程序的电脑连接同一个局域网。
3. 在电视或手机浏览器打开 `http://192.168.10.7:8999/tvbox.json`，能看到 JSON 就说明网络通路正常。
4. 打开 TVBox 设置，找到“配置地址”“订阅地址”或“接口”。
5. 如果填配置地址，填写 `http://192.168.10.7:8999/tvbox.json`。
6. 如果 TVBox 只允许填点播接口，填写 `http://192.168.10.7:8999/api/tvbox`。
7. 保存后返回首页并重新加载配置。TVBox 成功后会显示站点“果果剧库”。

如果 TVBox 在电视上提示无法访问，不要使用 `127.0.0.1`，那会指向电视自己；应使用运行程序的 Mac 局域网 IP，并检查 macOS 防火墙是否放行 TCP 8999。

TVBox 配置接口返回示例：

```json
{"lives":[],"sites":[{"api":"http://192.168.10.7:8999/api/tvbox","filterable":0,"key":"guoguo","name":"果果剧库","quickSearch":1,"searchable":1,"type":1}]}
```

### 9.3 播放和最高画质

TVBox 播放地址使用：

```text
/api/emby/stream.m3u8
```

程序给 TVBox 生成的播放请求会自动携带：

```text
client=tvbox
quality=highest
```

最高画质规则：

1. 源站提供 `1080p` 和 `720p` 时，选择 `1080p`
2. 源站提供更高分辨率且程序支持时，选择最高兼容档位
3. 只有 `720p` 时正常回退到 `720p`
4. HLS 主播放列表中有多个分辨率时，选择最高分辨率
5. 播放会话按 `quality=highest` 单独复用，不会误用普通网页播放器的较低画质会话

如果 TVBox 仍播放旧画质，请刷新 TVBox 配置、清播放缓存并确认服务已经重启到新版本。

## 10. 海报和图片代理

网页和 Emby 的海报通过程序内部图片接口转发：

```text
/api/ui/image?url=...
/api/emby/cover
```

程序不会让浏览器直接访问任意图片地址。图片地址必须来自当前账号可访问的剧库记录，随后程序再执行以下检查：

- 只允许 HTTPS，默认端口为 443
- 拒绝用户名和密码形式的 URL
- 拒绝本机、内网、链路本地、保留地址和危险重定向
- 拒绝 DNS 解析到私网后再重新绑定的地址
- 保留来源站点的 Referer 和必要请求头
- 校验响应确实是图片格式
- 支持缓存和失败退避

### 10.1 fake-IP 兼容

Clash、Surge、Mihomo 等代理工具可能启用 fake-IP DNS。此时公开 CDN 域名会解析成：

```text
198.18.0.20
198.18.0.21
198.18.0.22
198.18.0.23
```

代理还可能返回 IPv4-translated 形式：

```text
::ffff:0:c612:16
```

如果程序直接拒绝 `198.18.0.0/15`，所有海报都会失败并记录：

```text
封面地址指向本机、内网或保留网络
```

当前版本已经支持：

- 识别 `198.18.0.0/15` fake-IP
- 将 `::ffff:0:xxxx:xxxx` 规范化为对应 IPv4
- 仅对真实域名解析结果放开 fake-IP
- 仍然拒绝用户直接填写 `198.18.x.x` 图片 URL
- 仍然拒绝真实私网、回环和保留地址

## 11. 下载和合并

下载支持：

- 默认最高可用画质或指定画质
- 下载并发
- 分片重试和断点续传
- 暂停、恢复、重试、取消
- 任务持久化和重启恢复
- 自动选择兼容的视频、音频和封装格式
- 合并前校验文件是否完整
- 合并时优先复制或换封装
- 必要时只处理少数不兼容分集

下载画质不是强行放大。源站只有 `720p` 时不会生成真正的 `1080p`，程序会在日志中记录请求档位、可用档位和实际画质。

## 12. 数据目录和备份

必须备份：

```text
data/accounts.json
data/viewer-key
data/viewers/
data/emby-key
data/juku.json
data/playback-settings.json
data/ui-state.json
data/library.json
短剧下载/
```

建议在停止服务后备份 `data/`，避免复制过程中证书、密钥或任务状态发生变化。

不要公开分享以下内容：

- `accounts.json`
- `viewer-key`
- `viewers/`
- `emby-key`
- 日志中的签名 URL
- 代理账号密码
- Emby API Key
- 个人观看记录
- 下载的视频文件

## 13. Docker 部署

构建并启动：

```bash
docker compose up -d --build
```

默认映射：

```text
宿主机 8999 -> 容器 8999
```

数据卷：

```text
data -> /data
downloads -> /downloads
```

查看日志：

```bash
docker compose logs -f app
```

停止：

```bash
docker compose down
```

保留数据卷时不会删除账号、剧库和下载记录。升级后重新构建镜像即可。

可配置环境变量：

```text
JUKU_PORT
JUKU_BIND_ADDRESS
JUKU_PROXY_URL
JUKU_ADMIN_USER
JUKU_ADMIN_PASSWORD
JUKU_GO_IMAGE
JUKU_RUNTIME_IMAGE
TZ
```

## 14. 反向代理

反向代理需要：

- 转发 WebSocket 或长连接所需请求头
- 保留 Cookie 和 Set-Cookie
- 允许媒体 Range 请求
- 不要缓存 `/api/emby/stream.m3u8`
- 不要缓存 `/api/emby/segment.ts`
- 根据需要增加代理超时和上传限制

推荐保持同一访问域名，否则浏览器 Cookie、匿名身份和续播数据可能被视为新的访问者。

## 15. Emby 接入

Emby 支持：

- 导出 STRM 和 NFO
- 自动同步追剧或已下载合集
- 通知 Emby 扫描
- 通过签名 URL 获取海报
- 扫描完成后自动补集
- 保留用户自定义海报
- 失败退避和重启恢复

配置需要：

- Emby 服务器地址
- 有管理权限的 API Key
- 导出目录
- Emby 可以访问的程序地址
- 程序和 Emby 共享或可访问的输出目录

仅解压包含 NFO 的 ZIP 不会自动补海报。自动海报同步需要 Emby API，并且程序必须保持运行。

## 16. 编译和发布

### 16.1 本地编译

```bash
GOCACHE=/private/tmp/juku-gocache ./scripts/build-all.sh
```

脚本会生成：

```text
dist/juku_darwin_arm64
dist/juku_darwin_amd64
dist/juku_linux_arm64
dist/juku_linux_amd64
dist/juku_windows_arm64.exe
dist/juku_windows_amd64.exe
```

构建参数：

- `CGO_ENABLED=0`
- `GOTOOLCHAIN=local`
- `-trimpath`
- `-buildvcs=false`
- `-ldflags="-s -w"`

### 16.2 六平台打包

将二进制压缩为独立包或合集包后，可以生成校验文件：

```bash
shasum -a 256 release/v5/*.zip
```

发布包不要包含：

- `data/`
- 下载视频
- FFmpeg 便携缓存
- 日志
- 用户 Cookie
- API Key
- 管理员密码

### 16.3 GitHub Actions

仓库中的 `.github/workflows/docker-publish.yml` 会在推送到默认分支时构建并推送 Linux amd64 / arm64 Docker 镜像到 GitHub Container Registry。

## 17. 命令行参数

```text
-ui=true|false
-listen 地址
-open=true|false
-admin-user 用户名
-admin-password 密码
-mode all|search|id|list
-keyword 关键词
-id 剧ID
-out 目录
-c 并发数
-pages 页数
-data-dir 目录
-config 文件
-ffmpeg 路径
-refresh
-more
-insecure-tls=true|false
-help
```

查看完整帮助：

```bash
./juku -help
```

常见命令行示例：

```bash
./juku -ui=false -mode list
./juku -ui=false -mode list -refresh
./juku -ui=false -mode list -more
./juku -ui=false -mode search -keyword '关键词'
./juku -ui=false -mode id -id 'hongguo:实际剧ID'
```

`-refresh` 和 `-more` 不能同时使用。

## 18. 安全说明

- 默认匿名开放仅适合本机或可信局域网。
- 暴露到公网前建议启用必须登录、关闭自行注册，并使用强密码。
- 建议放在 HTTPS 反向代理后面。
- 不要把 `data/`、日志、Cookie 或签名密钥提交到 GitHub。
- 不建议开启 `-insecure-tls`，除非明确知道风险。
- 不要将服务端口直接暴露到不受信任网络。
- 分享程序时只分发可执行文件或源码，不要分享自己的运行目录。

## 19. 常见故障

### 19.1 网页没有图片

检查：

1. 服务是否是最新版本
2. `data/logs/app.log` 是否有 `cover.failed`
3. 代理工具是否启用了 fake-IP
4. 图片域名是否解析到 `198.18.x.x`
5. 服务是否在修改后重启
6. 反向代理是否允许图片接口返回二进制响应

fake-IP 场景下不应再出现“地址指向本机、内网或保留网络”，直接填写 fake-IP 地址仍然会被拒绝。

### 19.2 TVBox 还是 720p

检查：

1. TVBox 配置是否重新加载
2. 是否清除了旧播放缓存
3. 服务是否为当前 v5 版本
4. 日志是否记录源站确实提供 1080p
5. 播放地址是否包含 `quality=highest`

如果源站只返回 720p，程序无法凭空生成真实 1080p。

### 19.3 端口被占用

查看端口：

```bash
lsof -nP -iTCP:8999 -sTCP:LISTEN
```

替换为新端口：

```bash
./juku -listen 127.0.0.1:9000 -open=false
```

### 19.4 FFmpeg 不可用

程序会优先查找 PATH 和配置路径，必要时自动下载当前系统匹配的便携版本。下载失败时检查代理和网络，或在 Web UI 中修改 FFmpeg 路径。

### 19.5 播放卡顿或 CPU 高

- 检查线路和带宽
- 检查代理
- 查看 `playback.plan` 和 `playback.mode`
- 降低播放或转码并发
- 优先使用兼容的原始 MP4 / HLS
- 避免在低性能 VPS 上同时执行多个转码任务

### 19.6 Docker 能看列表但不能播放

检查容器中的 FFmpeg、代理、媒体 CDN 可达性，以及反向代理是否允许 Range 和 HLS 分片请求。不要只根据 Docker 容器内能看到网页就判断视频源可达。

### 19.7 登录后跳回登录页

检查：

- 是否使用同一个访问地址
- 反向代理是否转发 Cookie
- 浏览器是否禁用 Cookie
- HTTPS 页面是否配置了正确的安全 Cookie
- 数据卷是否在重启后被替换

## 20. 版本和验证

v5 节点包含：

- 六平台重新编译
- TVBox `quality=highest`
- 源站 1080p / 720p 最高分辨率选择
- HLS 多分辨率最高档选择
- TVBox 播放会话画质隔离
- fake-IP 海报兼容
- `::ffff:0:xxxx:xxxx` 地址规范化
- 图片 SSRF 防护保留
- 全量 Go 测试
- 发布包 SHA-256 校验

发布前建议执行：

```bash
GOCACHE=/private/tmp/juku-gocache go test ./... -count=1
./scripts/build-all.sh
shasum -a 256 -c release/v5/SHA256SUMS.txt
```

## 21. 已知边界

- 当前缓存数量不代表源站完整总量。
- 源站没有提供 1080p 时不能生成真实 1080p。
- fake-IP 兼容依赖域名解析结果和本机代理网络。
- 运行数据不能跨不同项目目录随意混用。
- 自动播放效果、移动端浏览器和 Emby 客户端仍会受到设备、网络和浏览器策略影响。
- 第三方站源可能随时调整接口、分页或媒体格式。
- 本仓库不承诺任何内容的版权授权，使用者需要自行确认合法使用范围。
